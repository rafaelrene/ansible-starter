---
# Every agent that gets the shared skills. Used to distribute skills and to
# prune links whose source skill is gone.
agents_skill_dirs:
  - ~/.config/opencode/skills
  - ~/.codex/skills
  - ~/.claude/skills
  - ~/.pi/agent/skills
